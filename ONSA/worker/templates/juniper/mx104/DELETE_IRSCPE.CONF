{% if pop_size == "large" %}
{% set logical_unit = {{ an_logical_unit }} %}
{% elif pop_size == "medium" %}	
{% set logical_unit = {{ client_logical_unit }} %}
{% endif %}

delete interfaces {{ an_uplink_interface }} unit {{ logical_unit }}
delete routing-instances INTERNET interface {{ an_uplink_interface }}.{{ logical_unit }}
delete routing-instances INTERNET routing-options static route {{ public_cidr }}